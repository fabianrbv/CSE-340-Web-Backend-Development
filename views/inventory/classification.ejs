<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <main>
    <!-- ====================== -->
    <!-- 1. Content Heading -->
    <!-- ====================== -->
    <% if (title) { %>
      <h1><%= title %></h1>
    <% } else { %>
      <% res.redirect('/') %>
    <% } %>

    <!-- ====================== -->
    <!-- 2. Message Block -->
    <!-- (currently commented) -->
    <!-- ====================== -->
    <%# messages() %>

    <!-- ====================== -->
    <!-- 3. Inventory Items Display -->
    <!-- ====================== -->
    <section id="inv-display">
      <%- grid %>
    </section>
  </main>
</body>
</html>
